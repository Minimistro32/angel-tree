<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Giving Tree</title>
    <link rel="stylesheet" type="text/css" href="../styles/normalize.css">
    <link rel="stylesheet" type="text/css" href="../styles/base.css">
    <link rel="stylesheet" href="../styles/form.css">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Dancing Script">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Inter">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Literata">
</head>

<body>
    <header>
        <p class="venmo">Venmo: @giving-tree</p>
        <h1>
            <div class="cursive">Take a Name</div>
            <div class="capital-font"> FROM THE </div>
            <div class="type-font">GIVING TREE</div>
        </h1>
    </header>
    <main>
    <section class="card donation-info">
      <h2>Our Friend’s Need</h2>
        if desc
          h3 #{desc}
      if img
        img(src=`/images/${img}`, alt=`Gift ${id}`, width="100px", class="center")
      //- THIS PART NEEDS TO BECOME A RANGE
      if suggest_price
        h4 Suggested Donation
        p $#{suggest_price} - $#{suggest_price}
      if url
        h4 An Example
        a(href = `${url}`) #{url}
      </section>

      <form action="thankyou.html" method="post">
            <fieldset>
                <legend>Claim this Gift</legend>
                <p class="formmsg"><strong class = "red">Important!</strong> - Provide an Amazon link (gift) to be purchased with your donation. This protects the anonymity of the recipient.</p>
                <label class="top" for="commit_price">Donation Amount ($):* <input class="donation-amount" type="number" name="commit_price" min=0, step="any" placeholder="Ex: 50.00" required></label>
                <label class="top" for="url">Amazon URL to Gift: <input type="url" id="url" name="url" placeholder="https://www.amazon.com/"></label>
                <label class="top" for="delivery_msg">Message to the Friend You're Sponsoring: <br> <textarea name="delivery_msg" cols="70" rows="10"></textarea></label>
                <label class="top" for="venmo">Venmo (We'll Venmo Request You)* <input type="venmo" id="venmo" name="venmo" required placeholder="Put your Venmo Here" ></label>
            </fieldset>
            <button type="submit" value="Claim" class="submitBtn" id="">Sponsor my Friend</button>
      </form>

      <div class="donate-btns">
        <ul>
          <li>
              a(href = `${nextUrl}`, class="button") Pick a Different Gift!
              <a href="/" ></a>
          </li>
          <li>
              <a href="/contribute/" class="button">Donate Directly</a>
          </li>
        </ul>
        </div>
    </main>
</body>
<footer>
</footer>

</html>
